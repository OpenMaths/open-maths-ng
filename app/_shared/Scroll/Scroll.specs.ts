module openmaths.specs {
    'use strict';

    describe('Scroll directive', () => {
        it('should have the directive constructor', () => {
            expect(openmaths.ScrollDirective.init).toBeDefined();
        });
    });
}