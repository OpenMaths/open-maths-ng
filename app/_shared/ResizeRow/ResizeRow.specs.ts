module openmaths.specs {
    'use strict';

    describe('ResizeRow directive', () => {
        it('should have the directive constructor', () => {
            expect(openmaths.ResizeRowDirective.init).toBeDefined();
        });
    });
}