module openmaths.specs {
    'use strict';

    describe('ContextMenu directive', () => {
        it('should have the directive constructor', () => {
         expect(openmaths.ContextMenuDirective.init).toBeDefined();
       });
    });
}